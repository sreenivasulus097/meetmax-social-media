import React from "react";
import Layout from "../../components/Layout";

const profile = () => {
  return <Layout pageTitle="Social Media - Profile">Profile</Layout>;
};

export default profile;
