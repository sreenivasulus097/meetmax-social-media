import React from "react";
import Layout from "../../components/Layout";

const settings = () => {
  return <Layout pageTitle="Social Media - Settings">Settings</Layout>;
};

export default settings;
